<UserControl x:Class="WeddingStoreDesktop.Views.ucDichVu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeddingStoreDesktop.Views"
             mc:Ignorable="d" Background="#80a7e5"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Danh sách loại dịch vụ" Grid.Column="0" Margin="5">
            <!--<ListView ItemsSource="{Binding LstLoaiDichVu, UpdateSourceTrigger=PropertyChanged}">
                <DataGrid>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã loại dịch vụ" Binding="{Binding MaLoaiDV}"/>
                        <DataGridTextColumn Header="Tên loại dịch vụ" Binding="{Binding TenLoaiDV}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ListView>-->
        </GroupBox>

        <GroupBox Header="Danh sách dịch vụ" Grid.Column="1" Margin="5">

        </GroupBox>
    </Grid>
</UserControl>
